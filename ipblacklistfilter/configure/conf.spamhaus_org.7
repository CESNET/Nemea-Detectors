# columns -- number of columns within each line of record (1..n)
# delimiter -- separating string between columns
# addr_col -- number specifying column containing address (1..n)
# prefixes -- boolean value specifying whether list contains IP ranges (0/1)
# domains -- boolean value specifying whether list contains domain names (0/1)

columns 2
delimiter ;
addr_col 1
prefixes 1
